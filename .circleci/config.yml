version: 2.1

jobs:
  noop:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: CircleCI workflow disabled
          command: |
            echo "CircleCI is no longer used for ergodocs."
            echo "This placeholder job exists so legacy CircleCI checks succeed."
            exit 0

workflows:
  version: 2
  noop-workflow:
    jobs:
      - noop
