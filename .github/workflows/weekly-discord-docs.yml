name: Weekly Discord Docs Leads

on:
  schedule:
    - cron: "0 9 * * 5"
  workflow_dispatch:
    inputs:
      days:
        description: "Days back to scan"
        required: false
        default: "7"
      after:
        description: "Override start date, YYYY-MM-DD"
        required: false

jobs:
  scan:
    name: Export chat leads and open issue
    runs-on: ubuntu-latest

    permissions:
      contents: read
      issues: write

    env:
      DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
      GITHUB_TOKEN: ${{ github.token }}
      GH_TOKEN: ${{ github.token }}
      GENERAL_CHANNEL: "668903786902847502"
      DEVELOPMENT_CHANNEL: "669989266478202917"
      REPORT_DIR: "weekly-discord-docs"
      SCAN_DAYS: ${{ github.event.inputs.days || '7' }}
      SCAN_AFTER: ${{ github.event.inputs.after || '' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install Python dependencies
        run: python -m pip install -r requirements.txt

      - name: Install DiscordChatExporter
        run: |
          mkdir -p /tmp/discordchatexporter
          gh release download \
            --repo Tyrrrz/DiscordChatExporter \
            --pattern "DiscordChatExporter.Cli.*linux-x64*.zip" \
            --dir /tmp/discordchatexporter
          unzip /tmp/discordchatexporter/DiscordChatExporter.Cli.*linux-x64*.zip -d /tmp/discordchatexporter
          chmod +x /tmp/discordchatexporter/DiscordChatExporter.Cli

      - name: Generate weekly lead reports
        id: reports
        run: |
          if [ -z "$DISCORD_TOKEN" ]; then
            echo "DISCORD_TOKEN repository secret is required." >&2
            exit 1
          fi

          if [ -n "$SCAN_AFTER" ]; then
            AFTER="$SCAN_AFTER"
          else
            AFTER="$(date -u -d "$SCAN_DAYS days ago" +%F)"
          fi

          BEFORE="$(date -u +%F)"
          mkdir -p "$REPORT_DIR"

          for CHANNEL in "general:$GENERAL_CHANNEL" "development:$DEVELOPMENT_CHANNEL"; do
            NAME="${CHANNEL%%:*}"
            ID="${CHANNEL##*:}"
            EXPORT_PATH="$(python tools/discord_dev_digest/discord_dev_digest.py export \
              --channel "$ID" \
              --after "$AFTER" \
              --before "$BEFORE" \
              --exporter /tmp/discordchatexporter/DiscordChatExporter.Cli \
              --output-dir "$REPORT_DIR")"
            for PROFILE in docs ecosystem github-links; do
              python tools/discord_dev_digest/discord_dev_digest.py analyze \
                --channel "$ID" \
                --after "$AFTER" \
                --before "$BEFORE" \
                --profile "$PROFILE" \
                --input "$EXPORT_PATH" \
                --output-dir "$REPORT_DIR"
            done
            printf "%s %s\n" "$NAME" "$ID" >> "$REPORT_DIR/channels.txt"
          done

          {
            echo "after=$AFTER"
            echo "before=$BEFORE"
            echo "issue_date=$BEFORE"
          } >> "$GITHUB_OUTPUT"

      - name: Open or update tracking issue
        id: tracking_issue
        run: |
          TITLE="Weekly Discord docs leads: ${{ steps.reports.outputs.issue_date }}"
          RUN_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

          {
            echo "Weekly Discord docs lead report."
            echo
            echo "Window: ${{ steps.reports.outputs.after }} to ${{ steps.reports.outputs.before }} UTC"
            echo
            echo "Channels:"
            echo "- general: $GENERAL_CHANNEL"
            echo "- development: $DEVELOPMENT_CHANNEL"
            echo
            echo "Reports are attached to the workflow artifact:"
            echo "$RUN_URL"
            echo
            echo "Maintainer workflow:"
            echo "1. Read the docs, ecosystem, and GitHub-links reports."
            echo "2. Treat Discord as leads only."
            echo "3. Verify claims against repositories, releases, issues, EIPs, or maintainer sources."
            echo "4. Update docs naturally. Do not mention the scan or chat export in public docs."
            echo "5. Run source_watch, nav_audit, structure_audit, git diff --check, and mkdocs build."
            echo "6. Open a PR with source links and verification notes."
          } > "$REPORT_DIR/issue-body.md"

          ISSUE_NUMBER="$(gh issue list --state open --search "$TITLE in:title" --json number,title --jq ".[] | select(.title == \"$TITLE\") | .number" | head -n 1)"

          if [ -n "$ISSUE_NUMBER" ]; then
            gh issue edit "$ISSUE_NUMBER" --body-file "$REPORT_DIR/issue-body.md"
          else
            ISSUE_URL="$(gh issue create --title "$TITLE" --body-file "$REPORT_DIR/issue-body.md")"
            ISSUE_NUMBER="${ISSUE_URL##*/}"
          fi

          echo "issue_number=$ISSUE_NUMBER" >> "$GITHUB_OUTPUT"

      - name: Scan watched source changes
        run: |
          python tools/source_watch.py scan \
            --github \
            --since "${{ steps.reports.outputs.after }}" \
            --format json \
            --output "$REPORT_DIR/source-watch.json" \
            --no-fail-on-changes

      - name: Upload reports
        uses: actions/upload-artifact@v4
        with:
          name: weekly-discord-docs-${{ steps.reports.outputs.issue_date }}
          path: weekly-discord-docs/

      - name: Open per-page source review issues
        id: per_page_issues
        run: |
          RUN_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          python tools/weekly_docs_prs.py \
            --report "$REPORT_DIR/source-watch.json" \
            --repo "${{ github.repository }}" \
            --artifact-url "$RUN_URL" \
            --output "$REPORT_DIR/per-page-issues.json"
          cat "$REPORT_DIR/per-page-issues.json"

      - name: Close tracking issue when no page review is needed
        run: |
          ISSUE_NUMBER="${{ steps.tracking_issue.outputs.issue_number }}"
          RUN_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          SUMMARY="$(python - <<'PY'
          import json
          from pathlib import Path

          data = json.loads(Path("weekly-discord-docs/per-page-issues.json").read_text(encoding="utf-8"))
          issues = data.get("issues", [])
          actionable = [item for item in issues if item.get("action") in {"created", "updated", "error"}]
          skipped = [item for item in issues if str(item.get("action", "")).startswith("skipped-")]
          print(json.dumps({
              "actionable": len(actionable),
              "skipped": len(skipped),
              "total": len(issues),
          }))
          PY
          )"
          ACTIONABLE="$(python -c 'import json,sys; print(json.loads(sys.argv[1])["actionable"])' "$SUMMARY")"
          SKIPPED="$(python -c 'import json,sys; print(json.loads(sys.argv[1])["skipped"])' "$SUMMARY")"
          TOTAL="$(python -c 'import json,sys; print(json.loads(sys.argv[1])["total"])' "$SUMMARY")"

          if [ "$ACTIONABLE" = "0" ]; then
            {
              echo "Automated review completed."
              echo
              echo "No per-page documentation review issues were needed."
              echo
              echo "Summary:"
              echo "- Candidates checked: $TOTAL"
              echo "- Skipped as already reviewed or low-signal: $SKIPPED"
              echo "- Actionable page issues: $ACTIONABLE"
              echo
              echo "Workflow run: $RUN_URL"
            } > "$REPORT_DIR/no-action-comment.md"
            gh issue comment "$ISSUE_NUMBER" --body-file "$REPORT_DIR/no-action-comment.md"
            gh issue close "$ISSUE_NUMBER"
          else
            echo "Keeping tracking issue #$ISSUE_NUMBER open because $ACTIONABLE actionable page issue(s) were created, updated, or errored."
          fi
