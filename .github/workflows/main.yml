name: CI

on:
  push:
    branches: [ main ]
  workflow_dispatch:

defaults:
  run:
    shell: bash
    working-directory: .

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Install SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.DEPLOY_KEY }}
          known_hosts: ${{ secrets.KNOWN_HOSTS }}

      - name: Test SSH Connection
        run: |
          echo "🔍 Testing SSH access..."
          ssh -v root@88.198.50.217 'echo "✅ SSH connection successful!"'

      - name: Debug Deploy Script Output
        run: |
          echo "🚀 Running deploy.sh in debug mode..."
          ssh root@88.198.50.217 'bash -x /var/www/html/ergodocs/tools/deploy.sh'

      - name: Run Deploy Script as Root
        run: |
          echo "🚀 Running deploy.sh as root..."
          ssh root@88.198.50.217 'sudo bash /var/www/html/ergodocs/tools/deploy.sh'

      - name: Verify Deployed Site on Remote Server
        run: |
       
